{% extends 'base.html.twig' %}

{% block title %}SubCategory{% endblock %}

{% block body %}
<div class="container">
<h1 class="mb-3 text-center">SubCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sub_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ sub_category.name }}</td>
            </tr>
        </tbody>
    </table>
     <div class="mb-3 gap-2">
      <a class="btn btn-primary" href="{{ path('app_sub_category_index') }}">back to list</a>

    <a class="btn btn-outline-success" href="{{ path('app_sub_category_edit', {'id': sub_category.id}) }}">edit</a>
     </div>
             {{ include('sub_category/_delete_form.html.twig') }}
    
</div>
    

{% endblock %}
